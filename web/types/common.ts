export type tImage = {
    altText:string;
    sourceUrl:string;
    title:string;
}

export type tButton = {
    title:string;
    url:string;
}

export type tFullname = {
    firstname:string;
    lastname:string;
}