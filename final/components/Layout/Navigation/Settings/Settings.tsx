import css from "nav/Settings/Settings.module.scss"
import {tSettings} from "nav/Settings/Settings.models"
import { Button } from "nav/index"

export default function Settings(){
    return (
        <div
            className={css.wrapper}
        >
            <Button/>
        </div>
    )
}